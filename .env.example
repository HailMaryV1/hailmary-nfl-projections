# Postgres connection used by every script in scripts/ (run_migration.py,
# scrapers, the projection engine). Get the real password from Supabase
# Dashboard -> Project Settings -> Database -> Connection string.
DATABASE_URL=postgresql://postgres.bfwcmwouaavefjfzgetv:REPLACE_ME@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

# Used by the frontend (Next.js) to talk to Supabase from the browser/server.
NEXT_PUBLIC_SUPABASE_URL=https://bfwcmwouaavefjfzgetv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_D0GfBSFEURivUIiSiiNbrg_kcpBgnvR

# Server-only, never exposed to the browser - Dashboard -> Project Settings -> API.
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME

# HTTP Basic Auth gate for the whole site (same pattern as Dream Team
# Projections) - leave unset locally to skip the gate.
SITE_PASSWORD=
